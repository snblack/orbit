# HW-0: Отчёт

### Проблемы:
1. Создавал Rails 8.1.3 приложение. При настройке окружения долго не мог решить конфликт трёх
Ruby-менеджеров (RVM, rbenv, mise) — потому что не указал сразу, что я использую терминал внутри RubyMine, а не обычный.